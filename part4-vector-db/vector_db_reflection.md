## Vector DB Use Case

Traditional keyword searches often pain taking with legal contracts because they only look for exact matches. `If a lawyer searches for "termination clauses," but the contract says "ending the agreement," the search will fail.`

A vector database solve uses semantic search, which focuses on the meaning behind words instead of just the spelling. Here is how it works:


Thus, a vector database plays a crucial role in enabling intelligent, context-aware search, making it far more effective than traditional keyword-based methods for large and complex legal documents.


